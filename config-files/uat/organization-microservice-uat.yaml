spring:
   jackson:
      serialization:
         INDENT_OUTPUT: true
   data:
      mongodb:
         host: ${MONGO_HOST}
         port: ${CCOMS_DATABASE_PORT}
         database: ${DATABASE}
         username: ${DB_USER}
         password: ${DB_PASSWD}
server:
  port: ${CCOMS_ORG_PORT}

emp:
   service:
      url:  http://${EMP_HOST}:${CCOMS_EMP_PORT}
dept:
   service:
       url: http://${DEPT_HOST}:${CCOMS_DEPT_PORT}

