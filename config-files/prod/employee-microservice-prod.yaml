spring:
   jackson:
      serialization:
         INDENT_OUTPUT: true
   data:
      mongodb:
         host: db.mongo.com
         port: ${CCOMS_DATABASE_PORT}
         database: ${DB_MONGO_SERVICENAME}
         username: ${CCOMS_DATABASE_USERNAME}
         password: ${CCOMS_DATABASE_PASSWORD}
server:
   port: ${CCOMS_EMP_PORT}
